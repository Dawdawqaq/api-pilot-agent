name: apipilot
services:
  apipilot-app:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - "127.0.0.1:${APP_PORT:-28080}:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=${SPRING_PROFILES_ACTIVE:-docker,deepseek}
      - DB_URL=jdbc:mysql://mysql:3306/dochelper?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
      - DB_USERNAME=${DB_USERNAME:-dochelper}
      - DB_PASSWORD=${DB_PASSWORD:?请设置数据库密码}
      - QDRANT_HOST=qdrant
      - QDRANT_GRPC_PORT=6334
      - QDRANT_HTTP_URL=http://qdrant:6333
      - MINIO_ENDPOINT=http://minio:9000
      - MINIO_ACCESS_KEY=${MINIO_ACCESS_KEY:?请设置 MinIO 用户名}
      - MINIO_SECRET_KEY=${MINIO_SECRET_KEY:?请设置 MinIO 密码}
      - MINIO_BUCKET=dochelper-files
      - SECRET_STORE_MASTER_KEY=${SECRET_STORE_MASTER_KEY:?请设置稳定主密钥}
      - AI_BASE_URL=${AI_BASE_URL:-https://api.deepseek.com}
      - AI_API_KEY=${AI_API_KEY:?请设置模型 API Key}
      - AI_CHAT_MODEL=${AI_CHAT_MODEL:-deepseek-flash}
      - AI_EMBEDDING_MODEL=${AI_EMBEDDING_MODEL:-deterministic-local}
      - AI_EMBEDDING_BASE_URL=${AI_EMBEDDING_BASE_URL:-}
      - AI_EMBEDDING_API_KEY=${AI_EMBEDDING_API_KEY:-}
      - AI_EMBEDDING_DIMENSIONS=${AI_EMBEDDING_DIMENSIONS:-}
    depends_on:
      mysql:
        condition: service_healthy
      qdrant:
        condition: service_started
      minio-init:
        condition: service_completed_successfully

  mysql:
    image: mysql:8.0.36
    restart: unless-stopped
    ports:
      - "127.0.0.1:33068:3306"
    environment:
      - MYSQL_ROOT_PASSWORD=${MYSQL_ROOT_PASSWORD:?请设置数据库管理密码}
      - MYSQL_DATABASE=dochelper
      - MYSQL_USER=${DB_USERNAME:-dochelper}
      - MYSQL_PASSWORD=${DB_PASSWORD:?请设置数据库密码}
    volumes:
      - mysql_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-p$${MYSQL_ROOT_PASSWORD:-root_password}"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 10s

  qdrant:
    image: qdrant/qdrant:v1.14.1
    restart: unless-stopped
    ports:
      - "127.0.0.1:16333:6333"
      - "127.0.0.1:16334:6334"
    volumes:
      - qdrant_data:/qdrant/storage

  minio:
    image: minio/minio:RELEASE.2024-05-10T01-41-38Z
    restart: unless-stopped
    command: server /data --console-address ":9001"
    ports:
      - "127.0.0.1:29000:9000"
      - "127.0.0.1:29001:9001"
    environment:
      - MINIO_ROOT_USER=${MINIO_ACCESS_KEY:?请设置 MinIO 用户名}
      - MINIO_ROOT_PASSWORD=${MINIO_SECRET_KEY:?请设置 MinIO 密码}
    volumes:
      - minio_data:/data
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 5s
      timeout: 3s
      retries: 5

  minio-init:
    image: minio/mc:RELEASE.2024-05-09T17-04-24Z
    environment:
      - MINIO_ACCESS_KEY=${MINIO_ACCESS_KEY:?请设置 MinIO 用户名}
      - MINIO_SECRET_KEY=${MINIO_SECRET_KEY:?请设置 MinIO 密码}
    depends_on:
      minio:
        condition: service_healthy
    entrypoint: >
      /bin/sh -c "
      /usr/bin/mc alias set myminio http://minio:9000 "$${MINIO_ACCESS_KEY}" "$${MINIO_SECRET_KEY}";
      /usr/bin/mc mb --ignore-existing myminio/dochelper-files;
      exit 0;
      "

volumes:
  mysql_data:
  qdrant_data:
  minio_data:
